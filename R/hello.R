# library(sparklyr)
# library(SparkWordCounter)
#
# sc <- spark_connect(master = "local")
#
# dir <- "/Users/Jared/GitHub/RPackageWrapperForSpark/SparkWordCounter/inst/java/"
# input_path <- paste0(dir, 'input_example.txt')
# ouput_path <- paste0(dir, 'output_example.txt')
#
# SparkWordCounter::count_words(input_path, output_path, sc)
#

